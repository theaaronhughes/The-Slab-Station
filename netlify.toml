[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"
  # Make bundling robust on Windows/OneDrive paths by treating these as externals
  external_node_modules = ["stripe", "resend"]
